name: Deploy Backend

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build \
            -t $ECR_REGISTRY/hajos-keepup:$IMAGE_TAG \
            -t $ECR_REGISTRY/hajos-keepup:latest \
            .
          docker push $ECR_REGISTRY/hajos-keepup:$IMAGE_TAG
          docker push $ECR_REGISTRY/hajos-keepup:latest

      - name: Deploy to ECS
        run: |
          aws ecs update-service \
            --cluster hajos-keepup-dev \
            --service hajos-keepup-dev \
            --force-new-deployment \
            --region us-east-2
