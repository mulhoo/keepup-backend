default: &default
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL", "redis://localhost:6379/1") %>
  channel_prefix: keepup

development:
  <<: *default

test:
  adapter: test

production:
  <<: *default
  url: <%= ENV["REDIS_URL"] %>
